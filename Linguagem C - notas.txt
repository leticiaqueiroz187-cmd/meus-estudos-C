Linguagem CQuestão 1 — Ler dois números inteiros e mostrar:soma, subtração, multiplicação, divisão#include <stdio.h>int main() {    int n1, n2;    float divisao;     scanf("%d %d", &n1, &n2);    divisao = (float)n1 / n2;    printf("Soma: %d\n", n1 + n2);    printf("Subtracao: %d\n", n1 - n2);    printf("Multiplicacao: %d\n", n1 * n2);    printf("Divisao: %.1f\n", divisao);    return 0;}#include <stdio.h>: Biblioteca usada para:printf : Mostrar na tela. ex: print("Hello, world");scanf : Ler dados ex: scanf("%d, &n1");int: Tipo de variável inteira. ex: int idade = 20;float: Tipo de variável decimal. ex: float nota = 8.5;%d: Inteiro.&: Pega o endereço da variável.\n: Quebra linha.Questão 2 — Ler 3 notas e calcular a média#include <stdio.h>int main() {    float n1, n2, n3, media;    scanf("%f %f %f", &n1, &n2, &n3);    media = (n1 + n2 + n3) / 3;    printf("Media = %.2f\n", media);    return 0;}Fórmula: media = (n1 + n2 + n3) / 3;Explicação%f: Números decimais.%.2f: Número com 2 casas decimais. ex: 8.17Questão 3 — Transformar segundos em: horas, minutos, segundos, restantes.#include <stdio.h>int main() {    int segundos;    int horas, minutos, resto;    scanf("%d", &segundos);    horas = segundos / 3600;    resto = segundos % 3600;    minutos = resto / 60;    resto = resto % 60;    printf("%d hora(s), %d minuto(s), %d segundo(s)\n", horas, minutos, resto);    return 0;}Explicação/: Divisão inteira.%: Resto da divisão.horas = segundos / 3600;resto = segundos % 3600;minutos = resto / 60;Segundos restantes (resto = resto % 60);Questão 4 — Calcular salário final com bônus de 10%.#include <stdio.h>int main() {    float salario, vendas;    float bonus, final;    scanf("%f %f", &salario, &vendas);    bonus = vendas * 0.10;    final = salario + bonus;    printf("Bonus = %.2f\n", bonus);    printf("Salario final = %.2f\n", final);    return 0;}Explicação0.10: 10%.bonus = vendas * 0.10;final = salario + bonus;Questão 5 — Par ou ímpar#include <stdio.h>int main() {    int num;    scanf("%d", &num);    if(num % 2 == 0) {        printf("Par\n");    }    else {        printf("Impar\n");    }    return 0;}Explicaçãoif: Seelse: Senãoelse if: Outra condiçãoNúmero par: num % 2 == 0 se o resto for 0 → número par.Número ímpar: Se o resto for diferente de 0.== Igualdade.Questão 6 — Mostrar qual número é maior.#include <stdio.h>int main() {    int a, b;    scanf("%d %d", &a, &b);    if(a > b) {        printf("%d é maior\n", a);    }    else if(b > a) {        printf("%d é maior\n", b);    }    else {        printf("Numeros iguais\n");    }    return 0;} Explicação>: Maior que.<: Menor que.Verifica se A é maior.DepoisVerifica se B é maior.SenãoOs números são iguais.Questão 7 — Classificar: criança, adolescente, idoso#include <stdio.h>int main() {    int idade;    scanf("%d", &idade);    if(idade <= 12) {        printf("Crianca\n");    }    else if(idade <= 17) {        printf("Adolescente\n");    }    else if(idade <= 59) {        printf("Adulto\n");    }    else {        printf("Idoso\n");    }    return 0;}Criança: idade <= 12Adolescente: idade <= 17Adulto: idade <= 59Idoso: Maior que 59Questão 8 - Aprovado ou reprovado ler a nota (0 a 10).#include <stdio.h>int main (){    int nota;     scanf("%d", &nota);     if(nota >= 7){        printf("Aprovado");    }    else if(nota >=5){        printf("Recuperação");    }    else {        printf("Reprovado");}    }Questão 9 -  contagem de 1 até N (for)#include <stdio.h>int main() {    int n;    printf("Digite um valor para N: ");    scanf("%d", &n);    for (int i = 1; i <= n; i++) {        printf("%d ", i);    }    printf("\n");    return 0;}i : Começa com 1i++ : Acrescenta 1Comandosint	inteirofloat	decimalchar	caractereprintf	mostrar na telascanf	ler dadosif	condiçãoelse	senãoelse if	outra condiçãofor	repetição com contadorwhile	repete enquantodo while executa antes de verificar%	resto da divisão/	divisão==	igual!=	diferente>	maior<	menor>=	maior ou igual<=	menor ou igualbreak	encerra laço\n	quebra linhai++	soma 1